# 🚂 Railway Deployment Guide

PocketOption Trading Bot полностью готов для развёртывания на Railway.

## 🚀 Быстрый старт

### 1. Подключение репозитория к Railway

```bash
# Установите Railway CLI
npm i -g @railway/cli

# Войдите в аккаунт
railway login

# Инициализируйте проект
railway init
```

Или просто:
1. Откройте https://railway.app
2. Нажмите "New Project" → "Deploy from GitHub"
3. Выберите репозиторий `manuninkirill-bot/pocketoption`

### 2. Установка переменных окружения

Railway автоматически обнаружит переменные из файла конфигурации. Добавьте дополнительно:

```bash
railway env add DATABASE_URL "postgresql://user:pass@host/db"
railway env add POCKET_OPTION_SSID "ваш_ssid"
railway env add SESSION_SECRET "ваш_secret"
```

Или через UI:
1. Перейдите на Dashboard проекта
2. Откройте Variables
3. Добавьте необходимые переменные

### 3. Развёртывание

```bash
# Автоматическое развёртывание при push в main
git push origin main

# Или ручное развёртывание
railway deploy
```

Railway автоматически:
- ✅ Клонирует репозиторий
- ✅ Устанавливает зависимости (`npm install`)
- ✅ Собирает приложение (`npm run build`)
- ✅ Запускает приложение (`npm run start`)
- ✅ Назначает публичный URL

## 📦 Как это работает

### Конфигурация

- **railway.json** - Основной конфиг с командами сборки и старта
- **.railway/nixpacks.toml** - Альтернативный конфиг сборки
- **Dockerfile** - Для контейнеризации (опционально)

### Порт

Railway использует переменную окружения `$PORT`. Приложение уже настроено:

```typescript
const port = parseInt(process.env.PORT || '5000', 10);
```

Railway автоматически выделит порт и установит `$PORT`.

### Переменные окружения

Railway автоматически загружает:
- `NODE_ENV=production` (из railway.json)
- `LOG_LEVEL=info` (из railway.json)
- `DATABASE_URL` (если подключена база)
- Все пользовательские переменные

## 🔌 Микросервисы

### Python Микросервис (po_service.py)

Railway поддерживает Python через nixpacks. Python автоматически установится:

1. Dockerfile включает `python3`
2. Микросервис запустится как часть приложения
3. Он будет доступен на `http://localhost:5001` внутри контейнера

**Для production:**
- Запустите Python сервис в отдельном процессе Railway
- Используйте Redis для кеширования между процессами

## 🗄️ База данных

### PostgreSQL на Railway

Railway предоставляет PostgreSQL как сервис:

```bash
# Добавьте PostgreSQL
railway add

# Выберите PostgreSQL
# Railway автоматически создаст DATABASE_URL
```

Или через UI:
1. Dashboard → Add a Service → Database
2. Выберите PostgreSQL
3. Переменные окружения установятся автоматически

### Миграции

```bash
# Перед первым развёртыванием
railway exec npm run db:push

# Или добавьте в package.json:
"postinstall": "npm run db:push"
```

## 🌐 Домены

Railway автоматически выделит поддомен:

```
https://pocketoption-production.up.railway.app
```

Для custom домена:
1. Dashboard → Settings → Custom Domain
2. Добавьте ваш домен
3. Следуйте инструкциям по DNS

## 📊 Мониторинг

Railway предоставляет встроенный мониторинг:

- **Logs** - Все логи приложения
- **Metrics** - CPU, память, сеть
- **Deployments** - История развёртываний
- **Activity** - События проекта

```bash
# Просмотр логов в CLI
railway logs

# Следование логам
railway logs --follow
```

## 🔒 Безопасность

### Secrets

Railway хранит переменные окружения шифрованными:

```bash
railway env add SECRET_KEY "value"  # Автоматически шифруется
```

### SSL/TLS

Railway автоматически предоставляет SSL сертификат для всех доменов.

## 🛠️ Troubleshooting

### Приложение крашится

```bash
# Проверьте логи
railway logs

# Убедитесь что PORT переменная используется
# Наше приложение это делает: const port = parseInt(process.env.PORT || '5000', 10);
```

### Python сервис не запускается

```bash
# Убедитесь что Python установлен в Dockerfile
# Текущий Dockerfile включает: RUN apk add --no-cache python3 py3-pip
```

### База данных не доступна

```bash
# Проверьте что DATABASE_URL установлена
railway env

# Убедитесь что база добавлена как сервис
railway service list
```

## 📈 Масштабирование

```bash
# Увеличьте memory
railway config --memory 2048

# Или через UI: Settings → Deploy → Compute
```

## 🚢 Production Checklist

- [x] railway.json создан
- [x] Dockerfile настроен
- [x] PORT переменная используется
- [x] NODE_ENV=production установлен
- [x] DATABASE_URL настроена
- [ ] POCKET_OPTION_SSID добавлена
- [ ] SESSION_SECRET установлена
- [ ] Custom домен (опционально)
- [ ] Бэкапы базы настроены
- [ ] Мониторинг и алерты включены

## 📚 Полезные команды

```bash
# Инициализация
railway init
railway link

# Деплой
railway deploy
railway up

# Переменные
railway env add KEY value
railway env remove KEY
railway env pull

# Логи
railway logs
railway logs --follow

# Просмотр
railway open
railway open --raw

# Конфигурация
railway config
railway config --memory 2048

# Удаление
railway down
```

## 📖 Документация

- **Railway Docs:** https://docs.railway.app
- **Railway CLI:** https://docs.railway.app/cli/commands
- **Node.js на Railway:** https://docs.railway.app/guides/nodejs

## 🎯 Результат

После успешного развёртывания:

```
✅ PocketOption Bot работает на Railway
✅ Автоматические обновления при push в GitHub
✅ PostgreSQL база данных
✅ Python микросервис для API
✅ Real-time WebSocket обновления
✅ Мониторинг и логирование
✅ SSL/TLS сертификаты
```

---

**Готово к production!** 🎉
